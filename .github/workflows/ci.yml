name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "23.13.1" # Or your preferred Node.js version

      - name: Install dependencies
        run: yarn install

      - name: Lint
        run: yarn lint
    
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "23.13.1" # Or your preferred Node.js version

      - name: Install dependencies
        run: yarn install

      - name: Run tests
        run: yarn test
